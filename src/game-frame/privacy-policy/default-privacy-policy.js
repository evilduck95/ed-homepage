const DefaultPrivacyPolicy = ({gameName}) => {
    return(
        <div>
            {gameName} collects no information about players, and never will.
        </div>
    )
}

export default DefaultPrivacyPolicy;